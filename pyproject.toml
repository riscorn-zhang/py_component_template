[project]
name = "py_component_template"
version = "0.1.0"
description = "Component-based Python project template."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["platformdirs>=4.9.6", "pluggy>=1.6.0"]
